<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="50dp"
        android:layout_height="40dp"
        android:src="@drawable/profile"
        android:id="@+id/message_profile_image"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="2dp"
        android:layout_toEndOf="@+id/message_profile_image"
        android:background="@drawable/reciver_meesage_text_background"
        android:text="receiver_message"
        android:textColor="@android:color/background_light"
        android:padding="10dp"
        android:textSize="14sp"
        android:textStyle="bold"
        android:id="@+id/receiver_message_id"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="2dp"
        android:layout_alignParentTop="@+id/message_profile_image"
        android:background="@drawable/message_sender_text_background"
        android:text="sender message"
        android:textColor="@android:color/background_light"
        android:padding="10dp"
        android:textSize="14sp"
        android:textStyle="bold"
        android:id="@+id/sender_message_id"/>


</RelativeLayout>